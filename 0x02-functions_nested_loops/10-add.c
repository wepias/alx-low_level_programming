#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @B: The first number to add
 * @A: The second number to add
 * Return: The sum of B and A
 */

int add(int B, int A)
{
	return (A + B);
}
